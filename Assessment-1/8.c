//printing one digit number over a three digit number.
#include<stdio.h>
int main(){
    int a;
    printf("Enter the number:");
    scanf("%d",&a);
    printf("The output is: %d",a%10);
    return 0;
}
